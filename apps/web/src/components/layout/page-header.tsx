import Link from 'next/link';
import { Icon, type IconName } from '@/components/ui';
export function PageHeader({ title, description, icon = 'building', helpHref, action }: { title: string; description: string; icon?: IconName; helpHref?: string; action?: React.ReactNode }) { return <header className="ui-card flex min-w-0 max-w-full flex-col gap-3 border-l-4 border-l-[var(--color-primary)] p-4 sm:flex-row sm:flex-wrap sm:items-center sm:justify-between"><div className="flex min-w-0 flex-1 items-center gap-3"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-full bg-[var(--color-primary)] text-[var(--color-on-primary)]"><Icon name={icon} /></span><div className="min-w-0 break-words"><h1 className="text-xl font-bold">{title}</h1><p className="text-[var(--color-text-secondary)]">{description}</p></div></div>{helpHref || action ? <div className="flex min-w-0 max-w-full flex-wrap items-center gap-2">{helpHref ? <Link className="btn btn-outline btn-compact" href={helpHref}>? Допомога</Link> : null}{action}</div> : null}</header>; }
